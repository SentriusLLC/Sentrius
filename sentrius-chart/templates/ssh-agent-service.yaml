apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ssh-agent
  namespace: {{ .Values.tenant }}
  annotations:
    {{- if eq .Values.environment "gke" }}
    cloud.google.com/backend-config: '{"default": "sentrius-backend-config"}'
    {{- else if eq .Values.environment "aws" }}
    {{- range $key, $value := .Values.sentrius.annotations.aws }}
    {{ $key }}: "{{ $value }}"
    {{- end }}
    {{- else if eq .Values.environment "azure" }}
    {{- range $key, $value := .Values.sentrius.annotations.azure }}
    {{ $key }}: "{{ $value }}"
    {{- end }}
    {{- end }}
spec:
  type: {{ .Values.sshagent.service.type }}
  ports:
    - name: http
      port: {{ .Values.sshagent.port }}
      targetPort: {{ .Values.sshagent.port }} # Port used inside the container
      {{- if eq .Values.sshagent.service.type "NodePort" }}
      nodePort: {{ .Values.sshagent.nodePort | default 30088 }}
      {{- end }}
  selector:
    app: ssh-agent